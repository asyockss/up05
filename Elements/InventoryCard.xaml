<UserControl x:Class="inventory.Elements.InventoryCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="140" d:DesignWidth="280">
    <Border BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="4" Margin="4" Background="White">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="{Binding Name}" FontWeight="Bold" FontSize="16"/>
            <TextBlock Grid.Row="1" Text="{Binding StartDate, StringFormat='Начало: {0:dd.MM.yyyy}'}" Margin="0,4,0,0" Foreground="#666666" FontSize="13"/>
            <TextBlock Grid.Row="2" Text="{Binding EndDate, StringFormat='Конец: {0:dd.MM.yyyy}'}" Margin="0,4,0,0" Foreground="#666666" FontSize="13"/>
        </Grid>
    </Border>
</UserControl>