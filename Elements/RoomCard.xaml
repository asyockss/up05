<UserControl x:Class="inventory.Elements.RoomCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="160" d:DesignWidth="280">
    <Border BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="4" Margin="4" Background="White">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="{Binding Name}" FontWeight="Bold" FontSize="16"/>
            <TextBlock Grid.Row="1" Text="{Binding ShortName, StringFormat='Краткое название: {0}'}" Margin="0,4,0,0" Foreground="#666666" FontSize="13"/>
            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,0">
                <Path Data="M12,4A4,4 0 0,1 16,8A4,4 0 0,1 12,12A4,4 0 0,1 8,8A4,4 0 0,1 12,4M12,14C16.42,14 20,15.79 20,18V20H4V18C4,15.79 7.58,14 12,14Z"
                      Width="16" Height="17" Fill="#666666" Stretch="Fill"/>
                <TextBlock Text="{Binding ResponsibleUser.FullName, TargetNullValue='Не указан'}" Margin="6,0,0,0" FontSize="13" TextTrimming="CharacterEllipsis"/>
            </StackPanel>
            <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,8,0,0">
                <Path Data="M12,4A4,4 0 0,1 16,8A4,4 0 0,1 12,12A4,4 0 0,1 8,8A4,4 0 0,1 12,4M12,14C16.42,14 20,15.79 20,18V20H4V18C4,15.79 7.58,14 12,14Z"
                      Width="16" Height="17" Fill="#666666" Stretch="Fill"/>
                <TextBlock Text="{Binding TempResponsibleUser.FullName, TargetNullValue='Не указан'}" Margin="6,0,0,0" FontSize="13" TextTrimming="CharacterEllipsis"/>
            </StackPanel>
            <TextBlock Grid.Row="4" Text="{Binding Equipment.Count, StringFormat='Оборудования: {0}'}" Margin="0,8,0,0" FontSize="12" Foreground="#666666"/>
        </Grid>
    </Border>
</UserControl>