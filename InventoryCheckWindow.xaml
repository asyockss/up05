<Window x:Class="inventory.InventoryCheckWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Проверка оборудования" Height="450" Width="600">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding SelectedInventory.Name}" FontSize="16" FontWeight="Bold"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding EquipmentList}" 
                  AutoGenerateColumns="False" IsReadOnly="True"
                  SelectedItem="{Binding SelectedEquipment}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Инв. номер" Binding="{Binding InventoryNumber}"/>
                <DataGridTextColumn Header="Аудитория" Binding="{Binding Room.Name}"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBox Grid.Row="2" Text="{Binding Comment}" 
                 Margin="0,10" Height="80" TextWrapping="Wrap"
                 AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Сохранить" Click="SaveCheck_Click" Width="100" Margin="5"/>
            <Button Content="Отмена" Click="Cancel_Click" Width="100" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>