<Page x:Class="inventory.Pages.AddEditInventoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d"
      d:DesignHeight="400" d:DesignWidth="600"
      Title="{Binding Title}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Title}" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Margin="10">
            <GroupBox Header="Информация об инвентаризации" Margin="0,0,0,10">
                <StackPanel>
                    <Label Content="Название"></Label>
                    <TextBox materialDesign:HintAssist.Hint="Название*" Text="{Binding CurrentInventory.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputFieldStyle}" Margin="0,0,0,10"/>
                    <Label Content="Дата начала"></Label>
                    <DatePicker materialDesign:HintAssist.Hint="Дата начала*" SelectedDate="{Binding CurrentInventory.StartDate, Mode=TwoWay}" DisplayDateStart="{Binding MinDate, Mode=OneWay}" DisplayDateEnd="{Binding MaxDate, Mode=OneWay}" Margin="0,0,0,10"/>
                    <Label Content="Дата окончания"></Label>
                    <DatePicker materialDesign:HintAssist.Hint="Дата окончания*" SelectedDate="{Binding CurrentInventory.EndDate, Mode=TwoWay}" DisplayDateStart="{Binding MinDate, Mode=OneWay}" DisplayDateEnd="{Binding MaxDate, Mode=OneWay}" Margin="0,0,0,10"/>
                    <Label Content="Отчетсвенный"></Label>
                    <ComboBox materialDesign:HintAssist.Hint="Пользователь*" ItemsSource="{Binding Users}" DisplayMemberPath="FullName" SelectedValuePath="Id" SelectedValue="{Binding CurrentInventory.UserId}" Style="{StaticResource ComboBoxStyle}" Margin="0,0,0,10"/>
                </StackPanel>
            </GroupBox>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Сохранить" Click="SaveButton_Click" Style="{StaticResource ActionButtonStyle}" Width="120"/>
            <Button Content="Отмена" Click="CancelButton_Click" Style="{StaticResource ActionButtonStyle}" Width="120" Margin="10,0,0,0"/>
        </StackPanel>
    </Grid>
</Page>