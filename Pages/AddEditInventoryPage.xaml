<Page x:Class="inventory.Pages.AddEditInventoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="400" d:DesignWidth="600"
      Title="{Binding Title}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Title}" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Margin="10">
            <GroupBox Header="Информация об инвентаризации" Margin="0,0,0,10">
                <StackPanel>
                    <TextBox Margin="0,0,0,10" Text="{Binding CurrentInventory.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    <DatePicker Margin="0,0,0,10" SelectedDate="{Binding CurrentInventory.StartDate, Mode=TwoWay}" DisplayDateStart="{Binding MinDate, Mode=OneWay}" DisplayDateEnd="{Binding MaxDate, Mode=OneWay}"/>
                    <DatePicker Margin="0,0,0,10" SelectedDate="{Binding CurrentInventory.EndDate, Mode=TwoWay}" DisplayDateStart="{Binding MinDate, Mode=OneWay}" DisplayDateEnd="{Binding MaxDate, Mode=OneWay}"/>
                    <ComboBox Margin="0,0,0,10" ItemsSource="{Binding Users}" DisplayMemberPath="FullName" SelectedValuePath="Id" SelectedValue="{Binding CurrentInventory.UserId}"/>
                </StackPanel>
            </GroupBox>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Сохранить" Click="SaveButton_Click" Width="120" Margin="0,0,10,0"/>
            <Button Content="Отмена" Click="CancelButton_Click" Width="120"/>
        </StackPanel>
    </Grid>
</Page>
