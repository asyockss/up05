<Page x:Class="inventory.Pages.InventoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="800"
      Title="Инвентаризация">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Панель инструментов -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <Button Content="Новая инвентаризация" Command="{Binding AddCommand}" Style="{StaticResource ToolButtonStyle}"/>
            <Button Content="Провести проверку" Command="{Binding PerformCheckCommand}" Style="{StaticResource ToolButtonStyle}"/>
            <Button Content="Отчет" Command="{Binding GenerateReportCommand}" Style="{StaticResource ToolButtonStyle}"/>

            <DatePicker SelectedDate="{Binding StartDateFilter}" Width="150" Margin="10,0,0,0"/>
            <DatePicker SelectedDate="{Binding EndDateFilter}" Width="150" Margin="5,0,0,0"/>
        </StackPanel>

        <ScrollViewer  VerticalScrollBarVisibility="Visible" Grid.Row="1">
            <StackPanel x:Name="parent">
                <!-- Здесь будут отображаться пользовательские элементы -->
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
